name: KMTC 스케줄 동기화

on:
  schedule:
    # 매시간 평일 KST (UTC -9). 평일 09~19시 KST = UTC 00~10시
    - cron: "0 23 * * 0-4"     # KST 평일 08:00
    - cron: "0 0 * * 1-5"      # KST 09:00
    - cron: "0 1 * * 1-5"      # KST 10:00
    - cron: "0 2 * * 1-5"      # KST 11:00
    - cron: "0 3 * * 1-5"      # KST 12:00
    - cron: "0 4 * * 1-5"      # KST 13:00
    - cron: "0 5 * * 1-5"      # KST 14:00
    - cron: "0 6 * * 1-5"      # KST 15:00
    - cron: "0 7 * * 1-5"      # KST 16:00
    - cron: "0 8 * * 1-5"      # KST 17:00
    - cron: "0 9 * * 1-5"      # KST 18:00
    - cron: "0 10 * * 1-5"     # KST 19:00
  workflow_dispatch:

jobs:
  kmtc-sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run kmtc_sync
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DS_ID: ${{ secrets.NOTION_DS_ID }}
          KMTC_PROXY_URL: ${{ secrets.KMTC_PROXY_URL }}
          PYTHONIOENCODING: utf-8
        run: |
          python scripts/kmtc_sync.py
